{
  "//": "Dependency direction: contracts <- shared <- {rtk-core, headroomd, plugin, eval}; plugin depends on contracts, shared, rtk, headroomd client surfaces only.",
  "name": "@bluecode/plugin",
  "version": "0.0.1",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@bluecode/contracts": "workspace:*",
    "@bluecode/shared": "workspace:*",
    "@bluecode/rtk": "workspace:*",
    "@bluecode/headroomd": "workspace:*",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.18.21",
    "@opencode-ai/sdk": "^1.18.21"
  }
}