{
  "//": "Dependency direction: contracts <- shared <- {rtk-core, headroomd, plugin, eval}; @bluecode/rtk-core may depend on @bluecode/shared only. No dependencies installed yet, by design.",
  "name": "@bluecode/rtk-core",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit"
  }
}
