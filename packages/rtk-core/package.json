{
  "//": "Dependency direction: contracts <- shared <- {rtk-core, headroomd, plugin, eval}; @bluecode/rtk-core may depend on @bluecode/contracts and @bluecode/shared only. External runtime deps: none (pure functions; hashing/sanitize/tokens come from @bluecode/shared).",
  "name": "@bluecode/rtk-core",
  "description": "Pure-function tool-output compression pipeline: classifier, six strategies, anchor protection, budget trimming",
  "keywords": [
    "compression",
    "pipeline",
    "classifier"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/EthyleneC2H4/Bluecode.git",
    "directory": "packages/rtk-core"
  },
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@bluecode/contracts": "workspace:^",
    "@bluecode/shared": "workspace:^"
  }
}
