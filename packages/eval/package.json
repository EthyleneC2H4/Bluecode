{
  "//": "Dependency direction: contracts <- shared <- {rtk-core, headroomd, rtk, plugin, eval}. Eval orchestrates the REAL rtk + headroomd clients (brief requirement), so unlike leaf packages it depends on both process packages; plugin precedent applies (leaf packages declare what they wire).",
  "name": "@bluecode/eval",
  "description": "A/B/C/D evaluation harness with exact token accounting and frozen-baseline regression gates",
  "keywords": [
    "evaluation",
    "benchmark",
    "regression-gates"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/EthyleneC2H4/Bluecode.git",
    "directory": "packages/eval"
  },
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@bluecode/contracts": "workspace:^",
    "@bluecode/shared": "workspace:^",
    "@bluecode/rtk": "workspace:^",
    "@bluecode/headroomd": "workspace:^"
  }
}
